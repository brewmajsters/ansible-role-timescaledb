---
# vars file for postgresql

debian_postgresql_dependencies:
  - postgresql-11
  - postgresql-server-dev-11

debian_timescaledb_dependencies:
  - timescaledb-postgresql-11

docker_dependencies:
  pip_packages:
    - docker
  packages:
    - gnupg2
    - apt-transport-https
    - ca-certificates
    - software-properties-common

docker_packages:
  - containerd.io
  - docker-ce
