---

arch: amd64
os: linux
dist: bionic

language: python
python:
  - "3.6"

services:
  - docker

install:
  - pip install molecule docker

script:
  # - molecule test
  - molecule test --scenario-name timescaledb_docker
